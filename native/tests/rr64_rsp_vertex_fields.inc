    dump(file, "posFloats", d.posFloats);
    dump(file, "velFloats", d.velFloats);
    dump(file, "tcFloats", d.tcFloats);
    dump(file, "tcVelFloats", d.tcVelFloats);
    dump(file, "normColBytes", d.normColBytes);
    dump(file, "viewProjIndices", d.viewProjIndices);
    dump(file, "worldIndices", d.worldIndices);
    dump(file, "fogIndices", d.fogIndices);
    dump(file, "lightIndices", d.lightIndices);
    dump(file, "lightCounts", d.lightCounts);
    dump(file, "lookAtIndices", d.lookAtIndices);
    dump(file, "faceIndices", d.faceIndices);
    dump(file, "modifyPosUints", d.modifyPosUints);
    dump(file, "posTransformed", d.posTransformed);
    dump(file, "posScreen", d.posScreen);
    dump(file, "rdpParams", d.rdpParams);
    dump(file, "extraParams", d.extraParams);
    dump(file, "renderParams", d.renderParams);
    dump(file, "viewTransforms", d.viewTransforms);
    dump(file, "projTransforms", d.projTransforms);
    dump(file, "viewProjTransforms", d.viewProjTransforms);
    dump(file, "modViewTransforms", d.modViewTransforms);
    dump(file, "modProjTransforms", d.modProjTransforms);
    dump(file, "modViewProjTransforms", d.modViewProjTransforms);
    dump(file, "prevViewTransforms", d.prevViewTransforms);
    dump(file, "prevProjTransforms", d.prevProjTransforms);
    dump(file, "prevViewProjTransforms", d.prevViewProjTransforms);
    dump(file, "worldTransforms", d.worldTransforms);
    dump(file, "prevWorldTransforms", d.prevWorldTransforms);
    dump(file, "invTWorldTransforms", d.invTWorldTransforms);
    dump(file, "lerpWorldTransforms", d.lerpWorldTransforms);
    dump(file, "rdpTiles", d.rdpTiles);
    dump(file, "lerpRdpTiles", d.lerpRdpTiles);
    dump(file, "gpuTiles", d.gpuTiles);
    dump(file, "callTiles", d.callTiles);
    dump(file, "rspViewports", d.rspViewports);
    dump(file, "viewportClipRatios", d.viewportClipRatios);
    dump(file, "viewportOrigins", d.viewportOrigins);
    dump(file, "rspFog", d.rspFog);
    dump(file, "rspLights", d.rspLights);
    dump(file, "rspLookAt", d.rspLookAt);
    dump(file, "lerpRspLookAt", d.lerpRspLookAt);
    dump(file, "loadOperations", d.loadOperations);
    dump(file, "triPosFloats", d.triPosFloats);
    dump(file, "triTcFloats", d.triTcFloats);
    dump(file, "triColorFloats", d.triColorFloats);
    dump(file, "transformGroups", d.transformGroups);
    dump(file, "worldTransformGroups", d.worldTransformGroups);
    dump(file, "viewProjTransformGroups", d.viewProjTransformGroups);
    dump(file, "worldTransformSegmentedAddresses", d.worldTransformSegmentedAddresses);
    dump(file, "worldTransformPhysicalAddresses", d.worldTransformPhysicalAddresses);
    dump(file, "worldTransformVertexIndices", d.worldTransformVertexIndices);
